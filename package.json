{
  "scripts": {
    "server": "babel-node --stage 0 app",
    "develop": "nodemon --ignore node_modules --exec npm run server -- app.js"
  },
  "dependencies": {
    "express": "^4.12.4",
    "request": "^2.58.0"
  },
  "devDependencies": {
    "babel": "^5.5.8",
    "nodemon": "^1.3.7"
  }
}
